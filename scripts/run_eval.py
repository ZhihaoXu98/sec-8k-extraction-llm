"""CLI: run the eval harness against a configured endpoint and write a report.

Drives :mod:`sec8k.eval.runner`; persists per-example artefacts plus a summary report
under ``eval_results/<timestamp>/``.
"""
